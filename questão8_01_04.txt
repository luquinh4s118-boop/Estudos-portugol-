programa
{
	funcao inicio ()
	{
	inteiro x,y,numero=0
	real media, soma=0, num
	
	escreva ("quantas notas voce desejar calcular? ")
	leia(x)
	
	para(y=1;y<=x;y++){
		escreva("digite a nota ")
		leia(num)
		soma = soma + num
		numero = numero + 1
		media = soma / x
		}
        escreva("a soma das notas é: ",soma,"\n")
	    escreva("a quantidade de numeros inteiros digitados foram",numero,"\n")
	    escreva ("a media aritmética das notas é: ",media,"\n")
		
		
}
}